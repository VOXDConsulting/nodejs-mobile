# CuidaMSG Node Android runtime build

This branch contains only the manually dispatched workflow used to build the
public Node.js Mobile source branch for CuidaMSG. It contains no CuidaMSG
application source, credentials, or user data.
